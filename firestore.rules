rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development / demo: open access. Tighten before production (auth + validation).
    match /buurten_collab/{document=**} {
      allow read, write: if true;
    }
  }
}
